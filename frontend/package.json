{
  "name": "projectbuddy-spa",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "React + TypeScript projects explorer consuming /api/v1 (served at /beta/projects behind FEATURE_SPA)",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc --noEmit && vite build && rm -f ../static/spa/index.html"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^8.2.1",
    "vitest": "^2.1.8"
  }
}
