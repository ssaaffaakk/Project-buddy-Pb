# Development / test dependencies (not needed in production).
#   pip install -r requirements-dev.txt
#   pytest
-r requirements.txt
pytest==8.4.2
pytest-cov==7.1.0
ruff==0.16.2
# Optional: experiment tracking for `flask train-recommender`
# (trainer logs to MLflow only when the package is installed)
# mlflow==2.22.2
